namespace: hello-kubernetes
docker:
  registry: ghcr.io/jotheesh-joe
  image:
    name: hello-kubernetes
    version: 8d5c6c704e049745527452772cc3e612e5e02cb3
  container:
    targetPort: 80
service:
  port: 8000
  protocol: http
deployment:
  replicas: 1
resources:
  requests:
    cpu: 200m
    memory: 4Gi
  limits:
    cpu: 2000m
    memory: 16Gi
istio:
  config:
    domain: hello-kubernetes.jotheesh-joe.com
    tls:
      enabled: true
      mode: simple
      secret: hello-kubernetes-secret
